#include "geometry.h"

int         _min(int a, int b)
{
    if (a < b)
        return (a);
    return (b);
}

int         _max(int a, int b)
{
    if (a > b)
        return (a);
    return (b);
}
